#include "main.h"

/**
 * int _strlen - returns the length of a string.
 *
 * @s: pointer
 * Return: void
 */
int _strlen(char *s)
{
	int length = 0;

	while (s[length] != '\0')
	{
		lenght++;

	}
	return (length);

}
