#include "main.h"

/**
 * int _abs - computes the absolute value 
 * of an integer.
 *
 * @n: number to be computed
 * Return: int
 */
int _abs(int n)
{
	int n;
	if (n > 0)
		return (n);
	else
		return (-n);
}
